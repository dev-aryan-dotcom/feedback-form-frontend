version: 1
applications:
  - appRoot: project
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist/project/browser
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
